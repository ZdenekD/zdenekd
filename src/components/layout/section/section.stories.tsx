import Section from '.';

export default {title: 'Layout/Section'};

export const component = () => (
    <Section>
        Section
    </Section>
);

component.parameters = {chromatic: {delay: 5000}};
