import Section from '.';

export default {
    title: 'Layout/Section',
    parameters: {chromatic: {delay: 50}},
};

export const component: React.FC = () => (
    <Section>
        Section
    </Section>
);
