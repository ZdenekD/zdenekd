import Layout from '.';

export default {
    title: 'Layout',
    parameters: {chromatic: {delay: 5000}},
};

export const component: React.FC = () => (
    <Layout>
        Layout content
    </Layout>
);
