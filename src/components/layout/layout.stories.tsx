import Layout from '.';

export default {
    title: 'Layout',
    parameters: {chromatic: {delay: 20}},
};

export const component: React.FC = () => (
    <Layout>
        Layout content
    </Layout>
);
