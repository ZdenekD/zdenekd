import Layout from '.';

export default {
    title: 'Layout',
    parameters: {chromatic: {delay: 50}},
};

export const component: React.FC = () => (
    <Layout>
        Layout content
    </Layout>
);
