import React from 'react';
import styles from './content.css';

const Content = () => (
    <section className={styles.content} data-test="component-content">
        Content
    </section>
);

export default Content;
