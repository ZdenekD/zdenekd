export type ICursor = {
    cursor: {
        isStuck: boolean,
        props: {
            width: number,
            height: number,
            top: number,
            left: number,
        }
    }
}
